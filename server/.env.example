# Copy this file to .env and paste your key (never commit .env).
# Gemini (recommended for dev):
GEMINI_API_KEY=your-key-here

# Optional model (default: gemini-3.1-flash-lite). Avoid 2.0/2.5 — blocked for new keys.
# GEMINI_MODEL=gemini-3.5-flash

# Optional — default 8787
# PORT=8787
